version: '3.8'
services:
  wolfsclaw-telegram:
    build: .
    container_name: wolfsclaw-telegram
    restart: always
    env_file: .env
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
